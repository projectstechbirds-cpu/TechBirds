{
  "name": "@techbirds/sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "generate": "echo \"TODO: openapi-typescript-codegen --input http://localhost:8002/openapi.json --output src/generated\"",
    "lint": "echo \"(sdk lint placeholder)\"",
    "typecheck": "tsc --noEmit",
    "build": "echo \"(consumed as source)\"",
    "test": "echo \"(no tests yet)\"",
    "clean": "rimraf .turbo dist"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
