/** Production hosts — keep in sync with `siteMeta` in `index.ts` and DNS. */
export const SITE_URLS = {
  marketing: "https://techbirdsgroup.com",
  blog: "https://blog.techbirdsgroup.com",
  app: "https://app.techbirdsgroup.com",
  api: "https://api.techbirdsgroup.com",
} as const;
